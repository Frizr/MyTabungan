import 'package:flutter/material.dart';

class AppColors {
  // Luxurious Minimalist Dark Theme
  static const Color background = Color(0xFF121212); // Deep rich black
  static const Color surface = Color(0xFF1E1E1E); // Elevated surface
  static const Color surfaceHighlight = Color(0xFF2C2C2C); // For cards or interactive elements

  // Accents (Gold/Champagne and subtle white)
  static const Color primary = Color(0xFFD4AF37); // Metallic Gold
  static const Color primaryVariant = Color(0xFFF3E5AB); // Vanilla/Champagne
  static const Color accent = Color(0xFFE0E0E0); // Crisp light gray for text

  // Text Colors
  static const Color textPrimary = Color(0xFFFFFFFF);
  static const Color textSecondary = Color(0xFFA0A0A0);

  // Status
  static const Color success = Color(0xFF4CAF50); // Muted green
  static const Color error = Color(0xFFE57373); // Muted red
}
